let fourSum = function (nums, target) {
    let solution = [];

    //sort  
    nums.sort(function(a,b) {return (a-b)})

    for(let i = 0; i < nums.length -2; i++) {
        //counters

        //while

            //if(tempRes = target)
    }
    
    return solution
};